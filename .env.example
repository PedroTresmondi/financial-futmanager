# Backend: use server.js (Express) em vez de sessionStorage
# Deixe comentado ou false para deploy estático (ex.: Vercel).
# VITE_USE_SERVER=true

# Chave de admin para rotas protegidas (só quando VITE_USE_SERVER=true)
# Deve ser a mesma do servidor (ADMIN_API_KEY).
# VITE_ADMIN_KEY=

# Servidor Express (quando rodar node server.js)
# PORT=3000
# ADMIN_API_KEY=
